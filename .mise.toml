[tools]
node = "22.16.0"

[tasks.audit]
run = "npm audit --audit-level=high"

[tasks.build]
run = "npm run build"

[tasks.lint]
run = "npm run lint"

[tasks.test]
run = "npm test"

[tasks.type-check]
run = "npm run type-check"
